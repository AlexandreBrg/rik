syntax = "proto3";

import "./messages.proto";

// The Worker service for the Scheduler 
service Scheduler {
  // Get worker and instances status updates.
  // Returns a stream of Status messages.
  rpc GetStatusUpdates(google.protobuf.Empty) returns (stream scheduler.Status);
}